# This file contains metadata for your plugin.

# This file should be included when you package your plugin.# Mandatory items:

[general]
name=Hyperspectral gaps filler
qgisMinimumVersion=3.14
description=QGIS Processing toolbox for hyperspectral and UAV-derived raster data: mosaicking, gap filling, frame filtering, raw airborne georeferencing and quality assessment.
version=0.2
author=Duke
email=st087204@student.spbu.ru

about=Processing framework for building gap-filled orthomosaics from hyperspectral/UAV remote sensing raster data with spectral consistency preservation. Provides algorithms for frame filtering, raw airborne georeferencing, mosaicking, gap filling and quality assessment. Requires rasterio >= 1.3 (uses dtype= / nodata= kwargs on rasterio.merge.merge); scipy and scikit-image are optional.

tracker=https://github.com/Burunduke/gaps_filler/issues
repository=https://github.com/Burunduke/gaps_filler
# End of mandatory metadata

# Recommended items:

hasProcessingProvider=yes
# Uncomment the following line and add your changelog:
# changelog=

# Tags are comma separated with spaces allowed
tags=raster, hyperspectral, orthophoto, gdal, nodata, fillnodata

homepage=https://github.com/Burunduke/gaps_filler
category=Plugins
icon=icon.png
# experimental flag
experimental=True

# deprecated flag (applies to the whole plugin, not just a single version)
deprecated=False

# Since QGIS 3.8, a comma separated list of plugins to be installed
# (or upgraded) can be specified.
# Check the documentation for more information.
# plugin_dependencies=

# category=

# If the plugin can run on QGIS Server.
server=False

